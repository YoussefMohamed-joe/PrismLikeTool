# Vogue Manager Production Pipeline System
# Core dependencies

# Qt GUI framework (PyQt6 is primary, PySide2 and PyQt5 as fallbacks)
PyQt6>=6.0.0
# PySide2>=5.15.0  # Uncomment if PyQt6 is not available
# PyQt5>=5.15.0  # Uncomment if PyQt6 and PySide2 are not available

# Data validation and serialization
pydantic>=1.8.0
jsonschema>=3.2.0

# Optional: FastAPI for local HTTP API
fastapi>=0.68.0
uvicorn>=0.15.0

# Image processing for thumbnails
Pillow>=8.0.0

# File system monitoring for automatic thumbnail generation
watchdog>=2.1.0

# Testing framework
pytest>=6.0.0
pytest-cov>=2.12.0

# Development tools
black>=21.0.0
flake8>=3.9.0
mypy>=0.910
